[project]
name = "get-syncd"
version = "0.3.0"
description = "Save points for DaVinci Resolve — version your timeline, see what changed, jump back to any cut"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Get Syncd Contributors" }]
dependencies = [
    "opentimelineio>=0.17",
    "rich>=13",
    "Pillow>=10",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-cov"]

[project.scripts]
get-syncd = "get_syncd.cli:main"
reeltrace = "get_syncd.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/get_syncd"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
